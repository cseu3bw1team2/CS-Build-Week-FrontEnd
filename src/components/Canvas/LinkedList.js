// class point {
//     constructor(props) {
//         this.value = props.value;
//         this.n_to = props.n_to ? props.n_to : null;
//         this.s_to = props.s_to ? props.s_to : null;
//         this.e_to = props.e_to ? props.e_to : null;
//         this.w_to = props.w_to ? props.w_to : null;
        
//     }
    
//     move = () => {

//     }
    
// }

// class QuadrupleLinkedList {
//     constructor(props) {
//         this.head = props.head ? props.head : null
//         this.tail = props.tail ? props.tail : null
//         this.length = 0;
//         this.value = props.value;
//         this.n_to = props.n_to ? props.n_to : null;
//         this.s_to = props.s_to ? props.s_to : null;
//         this.e_to = props.e_to ? props.e_to : null;
//         this.w_to = props.w_to ? props.w_to : null;
//     }

//     # Insert the given value into the tree
//     def insert(self, value):
//       if value < self.value:
//         if self.left is None:
//           self.left = BinarySearchTree(value)
//         else:
//           self.left.insert(value)
//       elif value >= self.value:
//         if self.right is None:
//           self.right = BinarySearchTree(value)
//         else:
//           self.right.insert(value)
//       return value

//     insert = (value) =>{
//         if(this.n_to) {
//             this.n_to = new QuadrupleLinkedList({value})
//         }
//     }

//     add_to_head = (value) => {
//     const newPoint = new Point({value});
//     this.length += 1;
//     if(!this.head && !this.self.tail) {
//         this.head = newPoint;
//     }
//     else { 
        
//         // newPoint.next = self.head
//         // self.head.prev = newNode
//         // self.head = newNode
//     }

//     return newNode.value  
// }


// }

// // get nodes 
