import Room from './Room'

class Point {
    constructor(props) {
        // this will serve as x,y coords
        this.x = props.n;
        this.y = props.s; // This is for the room the layer belongs to

    }

    move = (directionInfo) => {

    }
}

export default Position;